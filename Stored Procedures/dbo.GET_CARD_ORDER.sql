SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[GET_CARD_ORDER]
	@order int OUTPUT
AS

EXEC GET_SETTING_INT 'PC_ORDER', @order OUTPUT

UPDATE INI_INT
SET VALS = VALS + 1
WHERE IDS = 'PC_ORDER'

RETURN (0)
GO
