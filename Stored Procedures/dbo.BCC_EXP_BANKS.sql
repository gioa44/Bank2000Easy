SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO


CREATE PROCEDURE [dbo].[BCC_EXP_BANKS]
AS

SET NOCOUNT ON

SELECT CODE9, CODE3, DESCRIP, REC_STATE, DESCRIP_LAT, HEAD_CODE9
FROM dbo.BANKS (NOLOCK)
GO
