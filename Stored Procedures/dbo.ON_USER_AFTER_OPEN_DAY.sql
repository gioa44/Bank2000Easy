SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO

CREATE  PROCEDURE [dbo].[ON_USER_AFTER_OPEN_DAY]
	@in_tran bit
AS 
IF @in_tran <> 0 RETURN (0)

DECLARE 
	@r int

	EXEC @r = dbo.CONV_INCOME_OUTCOME_DEL 
	IF @@ERROR <> 0 OR @r <> 0 RETURN (1)
RETURN (0)

GO
